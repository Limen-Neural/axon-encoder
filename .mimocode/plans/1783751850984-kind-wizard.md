# Fix Plan: `feat/26-neuromod-gain-curves` Review Findings

## Scope
Fix all HIGH and MEDIUM findings from the code review of the `feat/26-neuromod-gain-curves` branch.

## Changes

### 1. LatencyEncoder: add `is_finite()` range guards (H3)
**File:** `src/encoders/latency.rs`
- In `new()` (line 29): change `assert!(range.0 < range.1, ...)` to `assert!(range.0.is_finite() && range.1.is_finite() && range.0 < range.1, ...)`
- In serde `Deserialize` (line 94): add finiteness check before `partial_cmp`, matching `PhaseEncoder::validate_params`
- Rename `clamp_and_normalize` → `normalize` (M7) since `f32::clamp` doesn't handle NaN — the name is misleading

### 2. Add `encode_with_modulators` to LatencyEncoder and PhaseEncoder (H4)
**Files:** `src/modulators.rs`, `src/encoders/latency.rs`, `src/encoders/phase.rs`

In `modulators.rs`:
- Add `latency` field to `ModulatorGainCurves` (line 136-142): `pub latency: Option<GainCurve>`
- Add `latency_scale` field to `EncodingGains` (line 144-150): `pub latency_scale: f32`
- Update `EncodingGains::identity()` to set `latency_scale: 1.0`
- Update `EncodingGains::sanitize()` to sanitize `latency_scale`
- Update serde `Deserialize` for `EncodingGains` to include `latency_scale`
- Update `NeuromodulatorGainCurves::apply_curves` to handle latency curve

In `latency.rs`:
- Add private helper `encode_with_latency_scale(&mut self, input: &[f32], latency_scale: f32)` that scales `max_latency` by the gain
- Add `encode_with_modulators` and `encode_step_with_modulators` following the pattern from delta.rs

In `phase.rs`:
- Add private helper `encode_with_sensitivity_scale(&mut self, input: &[f32], sensitivity_scale: f32)` that scales the output range
- Add `encode_with_modulators` and `encode_step_with_modulators`

### 3. Consistent `.max(0.0)` on effective threshold (M2)
**Files:** `src/encoders/predictive.rs`, `src/encoders/temporal.rs`
- In `predictive.rs` line 88: change `threshold * threshold_scale` to `(threshold * threshold_scale).max(0.0)`
- In `temporal.rs` line 83: change `threshold * threshold_scale` to `(threshold * threshold_scale).max(0.0)`
- Matches the pattern already used in `delta.rs` line 47

### 4. Fix test numbering and add missing tests (H5, H6)
**File:** `tests/serde_tests.rs`
- Fix duplicate `// 11.` comment at line 130 → `// 12.`
- Fix `// 12.` at line 136 → `// 13.`
- Add serde negative test for `GainCurve` with invalid `input_range` (min >= max)
- Add serde negative test for `GainCurve` with non-finite `output_range`
- Add serde roundtrip test for `LatencyEncoder` (currently missing)

### 5. Documentation improvements (H2, M1, M3)
**File:** `src/modulators.rs`
- Add doc comment on `GainCurve::evaluate()` noting negative levels are clamped to `input_range.0`
- Add doc comment on `EncodingGains` explaining asymmetric zero-gain semantics: zero threshold = max sensitivity, zero rate = silence

## Files Modified (in order)
1. `src/modulators.rs` — add latency_scale field, update identity/sanitize/serde/apply_curves
2. `src/encoders/latency.rs` — add is_finite guards, rename method, add encode_with_modulators
3. `src/encoders/phase.rs` — add encode_with_modulators
4. `src/encoders/predictive.rs` — add .max(0.0) to effective threshold
5. `src/encoders/temporal.rs` — add .max(0.0) to effective threshold
6. `tests/serde_tests.rs` — fix numbering, add missing tests

## Verification
1. `cargo test --locked` — all existing tests pass
2. `cargo test --features serde --locked` — serde tests pass
3. `cargo test --features "serde ndarray" --locked` — all features pass
4. `cargo fmt --check` — formatting clean
5. `cargo clippy --all-features -- -D warnings` — no warnings
